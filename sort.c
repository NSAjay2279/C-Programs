#include <stdio.h>
#include <cs50.h>

int main(void) {
    int n = get_int("Enter the value of n");
    for(int i=0; i<n; i++){
        int num[] = get_int("enter the %i numbers", n);
        
    }
}
